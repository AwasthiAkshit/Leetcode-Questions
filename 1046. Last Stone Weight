class Solution {
    public int lastStoneWeight(int[] stones) {
        PriorityQueue<Integer> p = new PriorityQueue<>(Collections.reverseOrder());
        for(int i=0;i<stones.length;i++){
            p.add(stones[i]);
        }
        while(p.size()>1){
            int y = p.remove();
            int x = p.remove();
            if(y!=x){
                p.add(y-x);
            }
        }
        if(p.size()==1) return p.remove();
        return 0;

    }
}
