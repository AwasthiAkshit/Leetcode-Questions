class Solution {
    public int subtractProductAndSum(int n) {
        int temp = n;
        int sum = 0;
        int pro = 1;
        while(temp!=0){
            int dig = temp%10;
            sum = sum+dig;
            pro = pro*dig;
            temp = temp/10;
        }
        return pro-sum;
    }
}
